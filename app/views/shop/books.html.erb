<h1>Shop</h1>
    <div class="row">
	<% @books.each do |m| %>
	<div class="col-sm-3">
	<%=link_to(image_tag(m.image.url, :style => "height:300px;width:150px;" ),   :class => "img-circle") %> <br>
     
	<a href="/page/<%=m.id%>">Quick View</a>
	</div>
	<% end %>
	</div>
	

